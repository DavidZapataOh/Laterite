/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
    combineCodec,
    containsBytes,
    fixEncoderSize,
    getBytesEncoder,
    getConstantDecoder,
    getConstantEncoder,
    getHiddenPrefixDecoder,
    getHiddenPrefixEncoder,
    getStructDecoder,
    getStructEncoder,
    getU64Decoder,
    getU64Encoder,
    type FixedSizeCodec,
    type FixedSizeDecoder,
    type FixedSizeEncoder,
    type ReadonlyUint8Array,
} from '@solana/kit';
import { getAttestationDecoder, getAttestationEncoder, type Attestation, type AttestationArgs } from '../types';

export const ATTESTED_EVENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([184, 102, 113, 199, 220, 197, 96, 50]);

export function getAttestedEventDiscriminatorBytes(): ReadonlyUint8Array {
    return fixEncoderSize(getBytesEncoder(), 8).encode(ATTESTED_EVENT_DISCRIMINATOR);
}

export type AttestedEvent = {
    attestation: Attestation;
    /** What the user's rule adds to `pending`. */
    invested: bigint;
    pending: bigint;
};

export type AttestedEventArgs = {
    attestation: AttestationArgs;
    /** What the user's rule adds to `pending`. */
    invested: number | bigint;
    pending: number | bigint;
};

/** Gets the encoder for {@link AttestedEventArgs} event data. */
export function getAttestedEventEncoder(): FixedSizeEncoder<AttestedEventArgs> {
    return getHiddenPrefixEncoder(
        getStructEncoder([
            ['attestation', getAttestationEncoder()],
            ['invested', getU64Encoder()],
            ['pending', getU64Encoder()],
        ]),
        [getConstantEncoder(ATTESTED_EVENT_DISCRIMINATOR)],
    );
}

/** Gets the decoder for {@link AttestedEvent} event data. */
export function getAttestedEventDecoder(): FixedSizeDecoder<AttestedEvent> {
    return getHiddenPrefixDecoder(
        getStructDecoder([
            ['attestation', getAttestationDecoder()],
            ['invested', getU64Decoder()],
            ['pending', getU64Decoder()],
        ]),
        [getConstantDecoder(ATTESTED_EVENT_DISCRIMINATOR)],
    );
}

/** Gets the codec for {@link AttestedEvent} event data. */
export function getAttestedEventCodec(): FixedSizeCodec<AttestedEventArgs, AttestedEvent> {
    return combineCodec(getAttestedEventEncoder(), getAttestedEventDecoder());
}

/** Parses the provided bytes into {@link AttestedEvent} event data. */
export function parseAttestedEvent(data: ReadonlyUint8Array | Uint8Array): AttestedEvent {
    if (containsBytes(data, ATTESTED_EVENT_DISCRIMINATOR, 0)) {
        return getAttestedEventDecoder().decode(data);
    }
    throw new Error('The provided data does not match the "AttestedEvent" event discriminators.');
}
