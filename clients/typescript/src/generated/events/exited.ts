/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
    combineCodec,
    containsBytes,
    fixEncoderSize,
    getAddressDecoder,
    getAddressEncoder,
    getBytesEncoder,
    getConstantDecoder,
    getConstantEncoder,
    getHiddenPrefixDecoder,
    getHiddenPrefixEncoder,
    getStructDecoder,
    getStructEncoder,
    type Address,
    type FixedSizeCodec,
    type FixedSizeDecoder,
    type FixedSizeEncoder,
    type ReadonlyUint8Array,
} from '@solana/kit';

export const EXITED_EVENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([219, 97, 107, 184, 198, 48, 50, 243]);

export function getExitedEventDiscriminatorBytes(): ReadonlyUint8Array {
    return fixEncoderSize(getBytesEncoder(), 8).encode(EXITED_EVENT_DISCRIMINATOR);
}

export type ExitedEvent = { user: Address };

export type ExitedEventArgs = ExitedEvent;

/** Gets the encoder for {@link ExitedEventArgs} event data. */
export function getExitedEventEncoder(): FixedSizeEncoder<ExitedEventArgs> {
    return getHiddenPrefixEncoder(getStructEncoder([['user', getAddressEncoder()]]), [
        getConstantEncoder(EXITED_EVENT_DISCRIMINATOR),
    ]);
}

/** Gets the decoder for {@link ExitedEvent} event data. */
export function getExitedEventDecoder(): FixedSizeDecoder<ExitedEvent> {
    return getHiddenPrefixDecoder(getStructDecoder([['user', getAddressDecoder()]]), [
        getConstantDecoder(EXITED_EVENT_DISCRIMINATOR),
    ]);
}

/** Gets the codec for {@link ExitedEvent} event data. */
export function getExitedEventCodec(): FixedSizeCodec<ExitedEventArgs, ExitedEvent> {
    return combineCodec(getExitedEventEncoder(), getExitedEventDecoder());
}

/** Parses the provided bytes into {@link ExitedEvent} event data. */
export function parseExitedEvent(data: ReadonlyUint8Array | Uint8Array): ExitedEvent {
    if (containsBytes(data, EXITED_EVENT_DISCRIMINATOR, 0)) {
        return getExitedEventDecoder().decode(data);
    }
    throw new Error('The provided data does not match the "ExitedEvent" event discriminators.');
}
