/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
    combineCodec,
    containsBytes,
    fixEncoderSize,
    getAddressDecoder,
    getAddressEncoder,
    getBytesEncoder,
    getConstantDecoder,
    getConstantEncoder,
    getHiddenPrefixDecoder,
    getHiddenPrefixEncoder,
    getI16Decoder,
    getI16Encoder,
    getStructDecoder,
    getStructEncoder,
    getU64Decoder,
    getU64Encoder,
    getU8Decoder,
    getU8Encoder,
    type Address,
    type FixedSizeCodec,
    type FixedSizeDecoder,
    type FixedSizeEncoder,
    type ReadonlyUint8Array,
} from '@solana/kit';

export const SWEPT_EVENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([254, 138, 9, 198, 192, 61, 165, 135]);

export function getSweptEventDiscriminatorBytes(): ReadonlyUint8Array {
    return fixEncoderSize(getBytesEncoder(), 8).encode(SWEPT_EVENT_DISCRIMINATOR);
}

export type SweptEvent = {
    user: Address;
    paymentToken: number;
    asset: number;
    /** Pulled for the engine and for pending amounts, in the payment token's raw units. */
    engine: bigint;
    pending: bigint;
    /**
     * The asset's verified price, `asset_price × 10^asset_exponent` dollars per 10^decimals raw units: the
     * display multiplier is included, so a UI token costs this price over the multiplier.
     */
    assetPrice: bigint;
    assetExponent: number;
    /** Raw asset units the user received, and the minimum the prices allowed. */
    received: bigint;
    minOut: bigint;
};

export type SweptEventArgs = {
    user: Address;
    paymentToken: number;
    asset: number;
    /** Pulled for the engine and for pending amounts, in the payment token's raw units. */
    engine: number | bigint;
    pending: number | bigint;
    /**
     * The asset's verified price, `asset_price × 10^asset_exponent` dollars per 10^decimals raw units: the
     * display multiplier is included, so a UI token costs this price over the multiplier.
     */
    assetPrice: number | bigint;
    assetExponent: number;
    /** Raw asset units the user received, and the minimum the prices allowed. */
    received: number | bigint;
    minOut: number | bigint;
};

/** Gets the encoder for {@link SweptEventArgs} event data. */
export function getSweptEventEncoder(): FixedSizeEncoder<SweptEventArgs> {
    return getHiddenPrefixEncoder(
        getStructEncoder([
            ['user', getAddressEncoder()],
            ['paymentToken', getU8Encoder()],
            ['asset', getU8Encoder()],
            ['engine', getU64Encoder()],
            ['pending', getU64Encoder()],
            ['assetPrice', getU64Encoder()],
            ['assetExponent', getI16Encoder()],
            ['received', getU64Encoder()],
            ['minOut', getU64Encoder()],
        ]),
        [getConstantEncoder(SWEPT_EVENT_DISCRIMINATOR)],
    );
}

/** Gets the decoder for {@link SweptEvent} event data. */
export function getSweptEventDecoder(): FixedSizeDecoder<SweptEvent> {
    return getHiddenPrefixDecoder(
        getStructDecoder([
            ['user', getAddressDecoder()],
            ['paymentToken', getU8Decoder()],
            ['asset', getU8Decoder()],
            ['engine', getU64Decoder()],
            ['pending', getU64Decoder()],
            ['assetPrice', getU64Decoder()],
            ['assetExponent', getI16Decoder()],
            ['received', getU64Decoder()],
            ['minOut', getU64Decoder()],
        ]),
        [getConstantDecoder(SWEPT_EVENT_DISCRIMINATOR)],
    );
}

/** Gets the codec for {@link SweptEvent} event data. */
export function getSweptEventCodec(): FixedSizeCodec<SweptEventArgs, SweptEvent> {
    return combineCodec(getSweptEventEncoder(), getSweptEventDecoder());
}

/** Parses the provided bytes into {@link SweptEvent} event data. */
export function parseSweptEvent(data: ReadonlyUint8Array | Uint8Array): SweptEvent {
    if (containsBytes(data, SWEPT_EVENT_DISCRIMINATOR, 0)) {
        return getSweptEventDecoder().decode(data);
    }
    throw new Error('The provided data does not match the "SweptEvent" event discriminators.');
}
