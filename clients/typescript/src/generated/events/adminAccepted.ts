/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
    combineCodec,
    containsBytes,
    fixEncoderSize,
    getAddressDecoder,
    getAddressEncoder,
    getBytesEncoder,
    getConstantDecoder,
    getConstantEncoder,
    getHiddenPrefixDecoder,
    getHiddenPrefixEncoder,
    getStructDecoder,
    getStructEncoder,
    type Address,
    type FixedSizeCodec,
    type FixedSizeDecoder,
    type FixedSizeEncoder,
    type ReadonlyUint8Array,
} from '@solana/kit';

export const ADMIN_ACCEPTED_EVENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
    174, 12, 76, 139, 158, 99, 110, 254,
]);

export function getAdminAcceptedEventDiscriminatorBytes(): ReadonlyUint8Array {
    return fixEncoderSize(getBytesEncoder(), 8).encode(ADMIN_ACCEPTED_EVENT_DISCRIMINATOR);
}

export type AdminAcceptedEvent = { admin: Address };

export type AdminAcceptedEventArgs = AdminAcceptedEvent;

/** Gets the encoder for {@link AdminAcceptedEventArgs} event data. */
export function getAdminAcceptedEventEncoder(): FixedSizeEncoder<AdminAcceptedEventArgs> {
    return getHiddenPrefixEncoder(getStructEncoder([['admin', getAddressEncoder()]]), [
        getConstantEncoder(ADMIN_ACCEPTED_EVENT_DISCRIMINATOR),
    ]);
}

/** Gets the decoder for {@link AdminAcceptedEvent} event data. */
export function getAdminAcceptedEventDecoder(): FixedSizeDecoder<AdminAcceptedEvent> {
    return getHiddenPrefixDecoder(getStructDecoder([['admin', getAddressDecoder()]]), [
        getConstantDecoder(ADMIN_ACCEPTED_EVENT_DISCRIMINATOR),
    ]);
}

/** Gets the codec for {@link AdminAcceptedEvent} event data. */
export function getAdminAcceptedEventCodec(): FixedSizeCodec<AdminAcceptedEventArgs, AdminAcceptedEvent> {
    return combineCodec(getAdminAcceptedEventEncoder(), getAdminAcceptedEventDecoder());
}

/** Parses the provided bytes into {@link AdminAcceptedEvent} event data. */
export function parseAdminAcceptedEvent(data: ReadonlyUint8Array | Uint8Array): AdminAcceptedEvent {
    if (containsBytes(data, ADMIN_ACCEPTED_EVENT_DISCRIMINATOR, 0)) {
        return getAdminAcceptedEventDecoder().decode(data);
    }
    throw new Error('The provided data does not match the "AdminAcceptedEvent" event discriminators.');
}
