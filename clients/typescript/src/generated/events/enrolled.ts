/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
    combineCodec,
    containsBytes,
    fixEncoderSize,
    getAddressDecoder,
    getAddressEncoder,
    getBytesEncoder,
    getConstantDecoder,
    getConstantEncoder,
    getHiddenPrefixDecoder,
    getHiddenPrefixEncoder,
    getStructDecoder,
    getStructEncoder,
    getU8Decoder,
    getU8Encoder,
    type Address,
    type FixedSizeCodec,
    type FixedSizeDecoder,
    type FixedSizeEncoder,
    type ReadonlyUint8Array,
} from '@solana/kit';

export const ENROLLED_EVENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([129, 156, 102, 214, 94, 196, 220, 127]);

export function getEnrolledEventDiscriminatorBytes(): ReadonlyUint8Array {
    return fixEncoderSize(getBytesEncoder(), 8).encode(ENROLLED_EVENT_DISCRIMINATOR);
}

export type EnrolledEvent = { user: Address; tier: number; paymentTokens: number; asset: number };

export type EnrolledEventArgs = EnrolledEvent;

/** Gets the encoder for {@link EnrolledEventArgs} event data. */
export function getEnrolledEventEncoder(): FixedSizeEncoder<EnrolledEventArgs> {
    return getHiddenPrefixEncoder(
        getStructEncoder([
            ['user', getAddressEncoder()],
            ['tier', getU8Encoder()],
            ['paymentTokens', getU8Encoder()],
            ['asset', getU8Encoder()],
        ]),
        [getConstantEncoder(ENROLLED_EVENT_DISCRIMINATOR)],
    );
}

/** Gets the decoder for {@link EnrolledEvent} event data. */
export function getEnrolledEventDecoder(): FixedSizeDecoder<EnrolledEvent> {
    return getHiddenPrefixDecoder(
        getStructDecoder([
            ['user', getAddressDecoder()],
            ['tier', getU8Decoder()],
            ['paymentTokens', getU8Decoder()],
            ['asset', getU8Decoder()],
        ]),
        [getConstantDecoder(ENROLLED_EVENT_DISCRIMINATOR)],
    );
}

/** Gets the codec for {@link EnrolledEvent} event data. */
export function getEnrolledEventCodec(): FixedSizeCodec<EnrolledEventArgs, EnrolledEvent> {
    return combineCodec(getEnrolledEventEncoder(), getEnrolledEventDecoder());
}

/** Parses the provided bytes into {@link EnrolledEvent} event data. */
export function parseEnrolledEvent(data: ReadonlyUint8Array | Uint8Array): EnrolledEvent {
    if (containsBytes(data, ENROLLED_EVENT_DISCRIMINATOR, 0)) {
        return getEnrolledEventDecoder().decode(data);
    }
    throw new Error('The provided data does not match the "EnrolledEvent" event discriminators.');
}
