/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
    combineCodec,
    containsBytes,
    fixEncoderSize,
    getAddressDecoder,
    getAddressEncoder,
    getBytesEncoder,
    getConstantDecoder,
    getConstantEncoder,
    getHiddenPrefixDecoder,
    getHiddenPrefixEncoder,
    getStructDecoder,
    getStructEncoder,
    getU64Decoder,
    getU64Encoder,
    type Address,
    type FixedSizeCodec,
    type FixedSizeDecoder,
    type FixedSizeEncoder,
    type ReadonlyUint8Array,
} from '@solana/kit';

export const PENDING_LOWERED_EVENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
    241, 51, 61, 199, 250, 212, 74, 199,
]);

export function getPendingLoweredEventDiscriminatorBytes(): ReadonlyUint8Array {
    return fixEncoderSize(getBytesEncoder(), 8).encode(PENDING_LOWERED_EVENT_DISCRIMINATOR);
}

export type PendingLoweredEvent = { user: Address; pending: bigint };

export type PendingLoweredEventArgs = { user: Address; pending: number | bigint };

/** Gets the encoder for {@link PendingLoweredEventArgs} event data. */
export function getPendingLoweredEventEncoder(): FixedSizeEncoder<PendingLoweredEventArgs> {
    return getHiddenPrefixEncoder(
        getStructEncoder([
            ['user', getAddressEncoder()],
            ['pending', getU64Encoder()],
        ]),
        [getConstantEncoder(PENDING_LOWERED_EVENT_DISCRIMINATOR)],
    );
}

/** Gets the decoder for {@link PendingLoweredEvent} event data. */
export function getPendingLoweredEventDecoder(): FixedSizeDecoder<PendingLoweredEvent> {
    return getHiddenPrefixDecoder(
        getStructDecoder([
            ['user', getAddressDecoder()],
            ['pending', getU64Decoder()],
        ]),
        [getConstantDecoder(PENDING_LOWERED_EVENT_DISCRIMINATOR)],
    );
}

/** Gets the codec for {@link PendingLoweredEvent} event data. */
export function getPendingLoweredEventCodec(): FixedSizeCodec<PendingLoweredEventArgs, PendingLoweredEvent> {
    return combineCodec(getPendingLoweredEventEncoder(), getPendingLoweredEventDecoder());
}

/** Parses the provided bytes into {@link PendingLoweredEvent} event data. */
export function parsePendingLoweredEvent(data: ReadonlyUint8Array | Uint8Array): PendingLoweredEvent {
    if (containsBytes(data, PENDING_LOWERED_EVENT_DISCRIMINATOR, 0)) {
        return getPendingLoweredEventDecoder().decode(data);
    }
    throw new Error('The provided data does not match the "PendingLoweredEvent" event discriminators.');
}
