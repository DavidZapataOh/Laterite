/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
    combineCodec,
    fixDecoderSize,
    fixEncoderSize,
    getBytesDecoder,
    getBytesEncoder,
    getStructDecoder,
    getStructEncoder,
    getU64Decoder,
    getU64Encoder,
    SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
    SolanaError,
    transformEncoder,
    type AccountMeta,
    type AccountSignerMeta,
    type Address,
    type FixedSizeCodec,
    type FixedSizeDecoder,
    type FixedSizeEncoder,
    type Instruction,
    type InstructionWithAccounts,
    type InstructionWithData,
    type ReadonlySignerAccount,
    type ReadonlyUint8Array,
    type WritableAccount,
} from '@solana/kit';
import {
    getAccountMetaFactory,
    getAddressFromResolvedInstructionAccount,
    type InstructionAccountInput,
    type InstructionAccountInputAddress,
    type InstructionSignerInput,
    type ResolvedInstructionAccount,
    type ResolvedInstructionAccountMeta,
} from '@solana/program-client-core';
import { findUserConfigPda } from '../pdas';
import { LATERITE_PROGRAM_ADDRESS } from '../programs';

export const LOWER_PENDING_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([6, 213, 208, 12, 142, 156, 34, 179]);

export function getLowerPendingDiscriminatorBytes(): ReadonlyUint8Array {
    return fixEncoderSize(getBytesEncoder(), 8).encode(LOWER_PENDING_DISCRIMINATOR);
}

export type LowerPendingInstruction<
    TProgram extends string = typeof LATERITE_PROGRAM_ADDRESS,
    TAccountUser extends string | AccountMeta<string> = string,
    TAccountUserConfig extends string | AccountMeta<string> = string,
    TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
    InstructionWithData<ReadonlyUint8Array> &
    InstructionWithAccounts<
        [
            TAccountUser extends string
                ? ReadonlySignerAccount<TAccountUser> & AccountSignerMeta<TAccountUser>
                : TAccountUser,
            TAccountUserConfig extends string ? WritableAccount<TAccountUserConfig> : TAccountUserConfig,
            ...TRemainingAccounts,
        ]
    >;

export type LowerPendingInstructionData = { discriminator: ReadonlyUint8Array; pending: bigint };

export type LowerPendingInstructionDataArgs = { pending: number | bigint };

export function getLowerPendingInstructionDataEncoder(): FixedSizeEncoder<LowerPendingInstructionDataArgs> {
    return transformEncoder(
        getStructEncoder([
            ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
            ['pending', getU64Encoder()],
        ]),
        value => ({ ...value, discriminator: LOWER_PENDING_DISCRIMINATOR }),
    );
}

export function getLowerPendingInstructionDataDecoder(): FixedSizeDecoder<LowerPendingInstructionData> {
    return getStructDecoder([
        ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
        ['pending', getU64Decoder()],
    ]);
}

export function getLowerPendingInstructionDataCodec(): FixedSizeCodec<
    LowerPendingInstructionDataArgs,
    LowerPendingInstructionData
> {
    return combineCodec(getLowerPendingInstructionDataEncoder(), getLowerPendingInstructionDataDecoder());
}

export type LowerPendingAsyncInput<
    TAccountUser extends InstructionSignerInput = InstructionSignerInput,
    TAccountUserConfig extends InstructionAccountInput = InstructionAccountInput,
> = {
    user: TAccountUser;
    userConfig?: TAccountUserConfig;
    pending: LowerPendingInstructionDataArgs['pending'];
};

export async function getLowerPendingInstructionAsync<
    TAccountUser extends InstructionSignerInput,
    TAccountUserConfig extends InstructionAccountInput,
    TProgramAddress extends Address = typeof LATERITE_PROGRAM_ADDRESS,
>(
    input: LowerPendingAsyncInput<TAccountUser, TAccountUserConfig>,
    config?: { programAddress?: TProgramAddress },
): Promise<
    LowerPendingInstruction<
        TProgramAddress,
        ResolvedInstructionAccountMeta<TAccountUser, InstructionAccountInputAddress<TAccountUser>>,
        ResolvedInstructionAccountMeta<TAccountUserConfig, InstructionAccountInputAddress<TAccountUserConfig>>
    >
> {
    // Program address.
    const programAddress = config?.programAddress ?? LATERITE_PROGRAM_ADDRESS;

    // Account meta helper.
    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');

    // Original accounts.
    const originalAccounts = {
        user: { value: input.user ?? null, isSigner: true, isWritable: false },
        userConfig: { value: input.userConfig ?? null, isSigner: false, isWritable: true },
    };
    const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;

    // Original args.
    const args = { ...input };

    // Resolve default values.
    if (!accounts.userConfig.value) {
        accounts.userConfig.value = await findUserConfigPda(
            { user: getAddressFromResolvedInstructionAccount('user', accounts.user.value) },
            { programAddress },
        );
    }

    return Object.freeze({
        accounts: [getAccountMeta('user', accounts.user), getAccountMeta('userConfig', accounts.userConfig)],
        data: getLowerPendingInstructionDataEncoder().encode(args as LowerPendingInstructionDataArgs),
        programAddress,
    } as LowerPendingInstruction<
        TProgramAddress,
        ResolvedInstructionAccountMeta<TAccountUser, InstructionAccountInputAddress<TAccountUser>>,
        ResolvedInstructionAccountMeta<TAccountUserConfig, InstructionAccountInputAddress<TAccountUserConfig>>
    >);
}

export type LowerPendingInput<
    TAccountUser extends InstructionSignerInput = InstructionSignerInput,
    TAccountUserConfig extends InstructionAccountInput = InstructionAccountInput,
> = {
    user: TAccountUser;
    userConfig: TAccountUserConfig;
    pending: LowerPendingInstructionDataArgs['pending'];
};

export function getLowerPendingInstruction<
    TAccountUser extends InstructionSignerInput,
    TAccountUserConfig extends InstructionAccountInput,
    TProgramAddress extends Address = typeof LATERITE_PROGRAM_ADDRESS,
>(
    input: LowerPendingInput<TAccountUser, TAccountUserConfig>,
    config?: { programAddress?: TProgramAddress },
): LowerPendingInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<TAccountUser, InstructionAccountInputAddress<TAccountUser>>,
    ResolvedInstructionAccountMeta<TAccountUserConfig, InstructionAccountInputAddress<TAccountUserConfig>>
> {
    // Program address.
    const programAddress = config?.programAddress ?? LATERITE_PROGRAM_ADDRESS;

    // Account meta helper.
    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');

    // Original accounts.
    const originalAccounts = {
        user: { value: input.user ?? null, isSigner: true, isWritable: false },
        userConfig: { value: input.userConfig ?? null, isSigner: false, isWritable: true },
    };
    const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;

    // Original args.
    const args = { ...input };

    return Object.freeze({
        accounts: [getAccountMeta('user', accounts.user), getAccountMeta('userConfig', accounts.userConfig)],
        data: getLowerPendingInstructionDataEncoder().encode(args as LowerPendingInstructionDataArgs),
        programAddress,
    } as LowerPendingInstruction<
        TProgramAddress,
        ResolvedInstructionAccountMeta<TAccountUser, InstructionAccountInputAddress<TAccountUser>>,
        ResolvedInstructionAccountMeta<TAccountUserConfig, InstructionAccountInputAddress<TAccountUserConfig>>
    >);
}

export type ParsedLowerPendingInstruction<
    TProgram extends string = typeof LATERITE_PROGRAM_ADDRESS,
    TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
    programAddress: Address<TProgram>;
    accounts: {
        user: TAccountMetas[0];
        userConfig: TAccountMetas[1];
    };
    data: LowerPendingInstructionData;
};

export function parseLowerPendingInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(
    instruction: Instruction<TProgram> &
        InstructionWithAccounts<TAccountMetas> &
        InstructionWithData<ReadonlyUint8Array>,
): ParsedLowerPendingInstruction<TProgram, TAccountMetas> {
    if (instruction.accounts.length < 2) {
        throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
            actualAccountMetas: instruction.accounts.length,
            expectedAccountMetas: 2,
        });
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
        accountIndex += 1;
        return accountMeta;
    };
    return {
        programAddress: instruction.programAddress,
        accounts: { user: getNextAccount(), userConfig: getNextAccount() },
        data: getLowerPendingInstructionDataDecoder().decode(instruction.data),
    };
}
