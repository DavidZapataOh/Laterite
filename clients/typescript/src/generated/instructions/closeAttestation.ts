/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
    combineCodec,
    fixDecoderSize,
    fixEncoderSize,
    getBytesDecoder,
    getBytesEncoder,
    getStructDecoder,
    getStructEncoder,
    SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
    SolanaError,
    transformEncoder,
    type AccountMeta,
    type Address,
    type FixedSizeCodec,
    type FixedSizeDecoder,
    type FixedSizeEncoder,
    type Instruction,
    type InstructionWithAccounts,
    type InstructionWithData,
    type ReadonlyUint8Array,
    type WritableAccount,
} from '@solana/kit';
import {
    getAccountMetaFactory,
    type InstructionAccountInput,
    type InstructionAccountInputAddress,
    type ResolvedInstructionAccount,
    type ResolvedInstructionAccountMeta,
} from '@solana/program-client-core';
import { LATERITE_PROGRAM_ADDRESS } from '../programs';

export const CLOSE_ATTESTATION_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
    249, 84, 133, 23, 48, 175, 252, 221,
]);

export function getCloseAttestationDiscriminatorBytes(): ReadonlyUint8Array {
    return fixEncoderSize(getBytesEncoder(), 8).encode(CLOSE_ATTESTATION_DISCRIMINATOR);
}

export type CloseAttestationInstruction<
    TProgram extends string = typeof LATERITE_PROGRAM_ADDRESS,
    TAccountRecord extends string | AccountMeta<string> = string,
    TAccountPayer extends string | AccountMeta<string> = string,
    TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
    InstructionWithData<ReadonlyUint8Array> &
    InstructionWithAccounts<
        [
            TAccountRecord extends string ? WritableAccount<TAccountRecord> : TAccountRecord,
            TAccountPayer extends string ? WritableAccount<TAccountPayer> : TAccountPayer,
            ...TRemainingAccounts,
        ]
    >;

export type CloseAttestationInstructionData = { discriminator: ReadonlyUint8Array };

export type CloseAttestationInstructionDataArgs = {};

export function getCloseAttestationInstructionDataEncoder(): FixedSizeEncoder<CloseAttestationInstructionDataArgs> {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]), value => ({
        ...value,
        discriminator: CLOSE_ATTESTATION_DISCRIMINATOR,
    }));
}

export function getCloseAttestationInstructionDataDecoder(): FixedSizeDecoder<CloseAttestationInstructionData> {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)]]);
}

export function getCloseAttestationInstructionDataCodec(): FixedSizeCodec<
    CloseAttestationInstructionDataArgs,
    CloseAttestationInstructionData
> {
    return combineCodec(getCloseAttestationInstructionDataEncoder(), getCloseAttestationInstructionDataDecoder());
}

export type CloseAttestationInput<
    TAccountRecord extends InstructionAccountInput = InstructionAccountInput,
    TAccountPayer extends InstructionAccountInput = InstructionAccountInput,
> = {
    record: TAccountRecord;
    payer: TAccountPayer;
};

export function getCloseAttestationInstruction<
    TAccountRecord extends InstructionAccountInput,
    TAccountPayer extends InstructionAccountInput,
    TProgramAddress extends Address = typeof LATERITE_PROGRAM_ADDRESS,
>(
    input: CloseAttestationInput<TAccountRecord, TAccountPayer>,
    config?: { programAddress?: TProgramAddress },
): CloseAttestationInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<TAccountRecord, InstructionAccountInputAddress<TAccountRecord>>,
    ResolvedInstructionAccountMeta<TAccountPayer, InstructionAccountInputAddress<TAccountPayer>>
> {
    // Program address.
    const programAddress = config?.programAddress ?? LATERITE_PROGRAM_ADDRESS;

    // Account meta helper.
    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');

    // Original accounts.
    const originalAccounts = {
        record: { value: input.record ?? null, isSigner: false, isWritable: true },
        payer: { value: input.payer ?? null, isSigner: false, isWritable: true },
    };
    const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;

    return Object.freeze({
        accounts: [getAccountMeta('record', accounts.record), getAccountMeta('payer', accounts.payer)],
        data: getCloseAttestationInstructionDataEncoder().encode({}),
        programAddress,
    } as CloseAttestationInstruction<
        TProgramAddress,
        ResolvedInstructionAccountMeta<TAccountRecord, InstructionAccountInputAddress<TAccountRecord>>,
        ResolvedInstructionAccountMeta<TAccountPayer, InstructionAccountInputAddress<TAccountPayer>>
    >);
}

export type ParsedCloseAttestationInstruction<
    TProgram extends string = typeof LATERITE_PROGRAM_ADDRESS,
    TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
    programAddress: Address<TProgram>;
    accounts: {
        record: TAccountMetas[0];
        payer: TAccountMetas[1];
    };
    data: CloseAttestationInstructionData;
};

export function parseCloseAttestationInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(
    instruction: Instruction<TProgram> &
        InstructionWithAccounts<TAccountMetas> &
        InstructionWithData<ReadonlyUint8Array>,
): ParsedCloseAttestationInstruction<TProgram, TAccountMetas> {
    if (instruction.accounts.length < 2) {
        throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
            actualAccountMetas: instruction.accounts.length,
            expectedAccountMetas: 2,
        });
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
        accountIndex += 1;
        return accountMeta;
    };
    return {
        programAddress: instruction.programAddress,
        accounts: { record: getNextAccount(), payer: getNextAccount() },
        data: getCloseAttestationInstructionDataDecoder().decode(instruction.data),
    };
}
