/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
    combineCodec,
    fixDecoderSize,
    fixEncoderSize,
    getArrayDecoder,
    getArrayEncoder,
    getBytesDecoder,
    getBytesEncoder,
    getStructDecoder,
    getStructEncoder,
    getU16Decoder,
    getU16Encoder,
    SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
    SolanaError,
    transformEncoder,
    type AccountMeta,
    type AccountSignerMeta,
    type Address,
    type Codec,
    type Decoder,
    type Encoder,
    type Instruction,
    type InstructionWithAccounts,
    type InstructionWithData,
    type ReadonlySignerAccount,
    type ReadonlyUint8Array,
    type WritableAccount,
} from '@solana/kit';
import {
    getAccountMetaFactory,
    type InstructionAccountInput,
    type InstructionAccountInputAddress,
    type InstructionSignerInput,
    type ResolvedInstructionAccount,
    type ResolvedInstructionAccountMeta,
} from '@solana/program-client-core';
import { findConfigPda } from '../pdas';
import { LATERITE_PROGRAM_ADDRESS } from '../programs';

export const SET_MARKET_CALENDAR_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
    232, 50, 197, 217, 121, 11, 11, 88,
]);

export function getSetMarketCalendarDiscriminatorBytes(): ReadonlyUint8Array {
    return fixEncoderSize(getBytesEncoder(), 8).encode(SET_MARKET_CALENDAR_DISCRIMINATOR);
}

export type SetMarketCalendarInstruction<
    TProgram extends string = typeof LATERITE_PROGRAM_ADDRESS,
    TAccountAdmin extends string | AccountMeta<string> = string,
    TAccountConfig extends string | AccountMeta<string> = string,
    TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
    InstructionWithData<ReadonlyUint8Array> &
    InstructionWithAccounts<
        [
            TAccountAdmin extends string
                ? ReadonlySignerAccount<TAccountAdmin> & AccountSignerMeta<TAccountAdmin>
                : TAccountAdmin,
            TAccountConfig extends string ? WritableAccount<TAccountConfig> : TAccountConfig,
            ...TRemainingAccounts,
        ]
    >;

export type SetMarketCalendarInstructionData = {
    discriminator: ReadonlyUint8Array;
    holidays: Array<number>;
    earlyCloses: Array<number>;
    validThrough: number;
};

export type SetMarketCalendarInstructionDataArgs = {
    holidays: Array<number>;
    earlyCloses: Array<number>;
    validThrough: number;
};

export function getSetMarketCalendarInstructionDataEncoder(): Encoder<SetMarketCalendarInstructionDataArgs> {
    return transformEncoder(
        getStructEncoder([
            ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
            ['holidays', getArrayEncoder(getU16Encoder())],
            ['earlyCloses', getArrayEncoder(getU16Encoder())],
            ['validThrough', getU16Encoder()],
        ]),
        value => ({ ...value, discriminator: SET_MARKET_CALENDAR_DISCRIMINATOR }),
    );
}

export function getSetMarketCalendarInstructionDataDecoder(): Decoder<SetMarketCalendarInstructionData> {
    return getStructDecoder([
        ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
        ['holidays', getArrayDecoder(getU16Decoder())],
        ['earlyCloses', getArrayDecoder(getU16Decoder())],
        ['validThrough', getU16Decoder()],
    ]);
}

export function getSetMarketCalendarInstructionDataCodec(): Codec<
    SetMarketCalendarInstructionDataArgs,
    SetMarketCalendarInstructionData
> {
    return combineCodec(getSetMarketCalendarInstructionDataEncoder(), getSetMarketCalendarInstructionDataDecoder());
}

export type SetMarketCalendarAsyncInput<
    TAccountAdmin extends InstructionSignerInput = InstructionSignerInput,
    TAccountConfig extends InstructionAccountInput = InstructionAccountInput,
> = {
    admin: TAccountAdmin;
    config?: TAccountConfig;
    holidays: SetMarketCalendarInstructionDataArgs['holidays'];
    earlyCloses: SetMarketCalendarInstructionDataArgs['earlyCloses'];
    validThrough: SetMarketCalendarInstructionDataArgs['validThrough'];
};

export async function getSetMarketCalendarInstructionAsync<
    TAccountAdmin extends InstructionSignerInput,
    TAccountConfig extends InstructionAccountInput,
    TProgramAddress extends Address = typeof LATERITE_PROGRAM_ADDRESS,
>(
    input: SetMarketCalendarAsyncInput<TAccountAdmin, TAccountConfig>,
    config?: { programAddress?: TProgramAddress },
): Promise<
    SetMarketCalendarInstruction<
        TProgramAddress,
        ResolvedInstructionAccountMeta<TAccountAdmin, InstructionAccountInputAddress<TAccountAdmin>>,
        ResolvedInstructionAccountMeta<TAccountConfig, InstructionAccountInputAddress<TAccountConfig>>
    >
> {
    // Program address.
    const programAddress = config?.programAddress ?? LATERITE_PROGRAM_ADDRESS;

    // Account meta helper.
    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');

    // Original accounts.
    const originalAccounts = {
        admin: { value: input.admin ?? null, isSigner: true, isWritable: false },
        config: { value: input.config ?? null, isSigner: false, isWritable: true },
    };
    const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;

    // Original args.
    const args = { ...input };

    // Resolve default values.
    if (!accounts.config.value) {
        accounts.config.value = await findConfigPda({ programAddress });
    }

    return Object.freeze({
        accounts: [getAccountMeta('admin', accounts.admin), getAccountMeta('config', accounts.config)],
        data: getSetMarketCalendarInstructionDataEncoder().encode(args as SetMarketCalendarInstructionDataArgs),
        programAddress,
    } as SetMarketCalendarInstruction<
        TProgramAddress,
        ResolvedInstructionAccountMeta<TAccountAdmin, InstructionAccountInputAddress<TAccountAdmin>>,
        ResolvedInstructionAccountMeta<TAccountConfig, InstructionAccountInputAddress<TAccountConfig>>
    >);
}

export type SetMarketCalendarInput<
    TAccountAdmin extends InstructionSignerInput = InstructionSignerInput,
    TAccountConfig extends InstructionAccountInput = InstructionAccountInput,
> = {
    admin: TAccountAdmin;
    config: TAccountConfig;
    holidays: SetMarketCalendarInstructionDataArgs['holidays'];
    earlyCloses: SetMarketCalendarInstructionDataArgs['earlyCloses'];
    validThrough: SetMarketCalendarInstructionDataArgs['validThrough'];
};

export function getSetMarketCalendarInstruction<
    TAccountAdmin extends InstructionSignerInput,
    TAccountConfig extends InstructionAccountInput,
    TProgramAddress extends Address = typeof LATERITE_PROGRAM_ADDRESS,
>(
    input: SetMarketCalendarInput<TAccountAdmin, TAccountConfig>,
    config?: { programAddress?: TProgramAddress },
): SetMarketCalendarInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<TAccountAdmin, InstructionAccountInputAddress<TAccountAdmin>>,
    ResolvedInstructionAccountMeta<TAccountConfig, InstructionAccountInputAddress<TAccountConfig>>
> {
    // Program address.
    const programAddress = config?.programAddress ?? LATERITE_PROGRAM_ADDRESS;

    // Account meta helper.
    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');

    // Original accounts.
    const originalAccounts = {
        admin: { value: input.admin ?? null, isSigner: true, isWritable: false },
        config: { value: input.config ?? null, isSigner: false, isWritable: true },
    };
    const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;

    // Original args.
    const args = { ...input };

    return Object.freeze({
        accounts: [getAccountMeta('admin', accounts.admin), getAccountMeta('config', accounts.config)],
        data: getSetMarketCalendarInstructionDataEncoder().encode(args as SetMarketCalendarInstructionDataArgs),
        programAddress,
    } as SetMarketCalendarInstruction<
        TProgramAddress,
        ResolvedInstructionAccountMeta<TAccountAdmin, InstructionAccountInputAddress<TAccountAdmin>>,
        ResolvedInstructionAccountMeta<TAccountConfig, InstructionAccountInputAddress<TAccountConfig>>
    >);
}

export type ParsedSetMarketCalendarInstruction<
    TProgram extends string = typeof LATERITE_PROGRAM_ADDRESS,
    TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
    programAddress: Address<TProgram>;
    accounts: {
        admin: TAccountMetas[0];
        config: TAccountMetas[1];
    };
    data: SetMarketCalendarInstructionData;
};

export function parseSetMarketCalendarInstruction<
    TProgram extends string,
    TAccountMetas extends readonly AccountMeta[],
>(
    instruction: Instruction<TProgram> &
        InstructionWithAccounts<TAccountMetas> &
        InstructionWithData<ReadonlyUint8Array>,
): ParsedSetMarketCalendarInstruction<TProgram, TAccountMetas> {
    if (instruction.accounts.length < 2) {
        throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
            actualAccountMetas: instruction.accounts.length,
            expectedAccountMetas: 2,
        });
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
        accountIndex += 1;
        return accountMeta;
    };
    return {
        programAddress: instruction.programAddress,
        accounts: { admin: getNextAccount(), config: getNextAccount() },
        data: getSetMarketCalendarInstructionDataDecoder().decode(instruction.data),
    };
}
