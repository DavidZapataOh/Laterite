/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
    combineCodec,
    fixDecoderSize,
    fixEncoderSize,
    getBytesDecoder,
    getBytesEncoder,
    getStructDecoder,
    getStructEncoder,
    getU16Decoder,
    getU16Encoder,
    type FixedSizeCodec,
    type FixedSizeDecoder,
    type FixedSizeEncoder,
    type ReadonlyUint8Array,
} from '@solana/kit';

/**
 * The NYSE closures the weekly engine respects, one bit per day: bit `i` of a bitmap (byte `i / 8`, bit `i % 8`)
 * stands for day `first_day + i`, counted from 1970-01-01.
 */
export type MarketCalendar = {
    /** The day the calendar was loaded. */
    firstDay: number;
    /** The last day the calendar covers; outside `first_day..=valid_through` the market counts as closed. */
    validThrough: number;
    /** Full-day closures. */
    holidays: ReadonlyUint8Array;
    /** Days the market closes at 13:00 New York time. */
    earlyCloses: ReadonlyUint8Array;
};

export type MarketCalendarArgs = MarketCalendar;

export function getMarketCalendarEncoder(): FixedSizeEncoder<MarketCalendarArgs> {
    return getStructEncoder([
        ['firstDay', getU16Encoder()],
        ['validThrough', getU16Encoder()],
        ['holidays', fixEncoderSize(getBytesEncoder(), 183)],
        ['earlyCloses', fixEncoderSize(getBytesEncoder(), 183)],
    ]);
}

export function getMarketCalendarDecoder(): FixedSizeDecoder<MarketCalendar> {
    return getStructDecoder([
        ['firstDay', getU16Decoder()],
        ['validThrough', getU16Decoder()],
        ['holidays', fixDecoderSize(getBytesDecoder(), 183)],
        ['earlyCloses', fixDecoderSize(getBytesDecoder(), 183)],
    ]);
}

export function getMarketCalendarCodec(): FixedSizeCodec<MarketCalendarArgs, MarketCalendar> {
    return combineCodec(getMarketCalendarEncoder(), getMarketCalendarDecoder());
}
