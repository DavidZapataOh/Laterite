/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
    combineCodec,
    fixDecoderSize,
    fixEncoderSize,
    getAddressDecoder,
    getAddressEncoder,
    getBytesDecoder,
    getBytesEncoder,
    getI64Decoder,
    getI64Encoder,
    getStructDecoder,
    getStructEncoder,
    getU16Decoder,
    getU16Encoder,
    getU64Decoder,
    getU64Encoder,
    getU8Decoder,
    getU8Encoder,
    type Address,
    type FixedSizeCodec,
    type FixedSizeDecoder,
    type FixedSizeEncoder,
    type ReadonlyUint8Array,
} from '@solana/kit';
import { getEventKindDecoder, getEventKindEncoder, type EventKind, type EventKindArgs } from '.';

/** A transfer the attestor observed, in the payment token's raw units. */
export type Attestation = {
    kind: EventKind;
    user: Address;
    /** Index into `Config.payment_tokens`. */
    paymentToken: number;
    amount: bigint;
    /** Block time of the transfer. */
    eventTime: bigint;
    /** The transfer's transaction signature. */
    signature: ReadonlyUint8Array;
    /**
     * Position of the transfer, from 0 in instruction order (each outer instruction followed by its inner ones),
     * among the transaction's transfers of either configured payment token to or from the user.
     */
    transferIndex: number;
};

export type AttestationArgs = {
    kind: EventKindArgs;
    user: Address;
    /** Index into `Config.payment_tokens`. */
    paymentToken: number;
    amount: number | bigint;
    /** Block time of the transfer. */
    eventTime: number | bigint;
    /** The transfer's transaction signature. */
    signature: ReadonlyUint8Array;
    /**
     * Position of the transfer, from 0 in instruction order (each outer instruction followed by its inner ones),
     * among the transaction's transfers of either configured payment token to or from the user.
     */
    transferIndex: number;
};

export function getAttestationEncoder(): FixedSizeEncoder<AttestationArgs> {
    return getStructEncoder([
        ['kind', getEventKindEncoder()],
        ['user', getAddressEncoder()],
        ['paymentToken', getU8Encoder()],
        ['amount', getU64Encoder()],
        ['eventTime', getI64Encoder()],
        ['signature', fixEncoderSize(getBytesEncoder(), 64)],
        ['transferIndex', getU16Encoder()],
    ]);
}

export function getAttestationDecoder(): FixedSizeDecoder<Attestation> {
    return getStructDecoder([
        ['kind', getEventKindDecoder()],
        ['user', getAddressDecoder()],
        ['paymentToken', getU8Decoder()],
        ['amount', getU64Decoder()],
        ['eventTime', getI64Decoder()],
        ['signature', fixDecoderSize(getBytesDecoder(), 64)],
        ['transferIndex', getU16Decoder()],
    ]);
}

export function getAttestationCodec(): FixedSizeCodec<AttestationArgs, Attestation> {
    return combineCodec(getAttestationEncoder(), getAttestationDecoder());
}
