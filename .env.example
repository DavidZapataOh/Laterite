# Mainnet RPC used as the fork's datasource (a free Helius or QuickNode endpoint works)
SURFPOOL_DATASOURCE_RPC_URL=
# Optional; keyless Jupiter access is limited to 0.5 requests per second
JUPITER_API_KEY=
