import styles from './field.module.css';

/**
 * The text field class, outlined like a chip: for an `<input>`, or for a `<label>` that holds inputs and their units
 * (the inputs inside lose their own box). The placement sets height, padding and font size.
 */
export const field = styles.field;
